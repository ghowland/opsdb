# dos/prod-0/importers/credentials/pagerduty.yaml

# PagerDuty Credentials — prod-0
#
# References the PagerDuty API token via environment variable.

pagerduty:
  api_token_env_var: OPSDB_PAGERDUTY_TOKEN
  # Optional: limit to specific services or teams
  service_filter: []
  team_filter: []


